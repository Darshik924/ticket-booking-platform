I am migrating my handwritten Tailwind project to a Shadcn semantic token system. 

Look at my custom pages like `events/page.tsx`, `admin/page.tsx`, `bookings/page.tsx`, `events/[id]/page.tsx`, `login/page.tsx` and `register/page.tsx`. For now Change these components Only. 

Without changing their HTML structure, functionality, or TypeScript logic, rewrite their Tailwind classes to use Shadcn's semantic variables. Map them intelligently:
- Replace hardcoded dark/black text/backgrounds with header/sidebar roles or `bg-background` / `text-foreground`.
- Replace hardcoded blues/accent colors with `bg-primary` / `text-primary`.
- Replace hardcoded gray borders with `border-border`.
- Replace hardcoded gray text with `text-muted-foreground`.
- Replace all the other elements that u see are hardcoded to the ones that are inside the new generated design system

Please output the changes into file contents or apply the edits directly.